#include <iostream>

using namespace std;

int main()
{
    int a;
    cout<<"enter a integer number";
    cin>>a;
    if(a>0)
        cout<<"the number is possitive:"<<a;
    else if(a>0)
        cout<<"The number is negative:"<<a;
}
